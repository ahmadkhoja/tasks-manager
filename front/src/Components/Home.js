import React from 'react'

 

export default class Home extends React.Component {
    // constructor(props){
    //     super(props)
    //     this.state = {}
    // }
    render(){
        return(
            <div>
                <h1>Home Page</h1>
                {/* { this.props.first_day_tasks.map( (task, i) =>
                        <div key={i}>
                            <h1> {task.name} </h1>
                            <h1> {task.priority} </h1>
                        </div>
                     
                )} */}
                
            </div>
        )
    }
}